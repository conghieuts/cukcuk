<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.com.misa.hieudc.cukcuklite">

    <application
        android:name=".config.AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="singleTask"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screen.reportdetailscreen.ReportDetailActivity"></activity>
        <activity android:name=".screen.checkoutscreen.CheckoutActivity" />
        <activity android:name=".screen.mainscreen.MainActivity" />
        <activity android:name=".screen.selectunitscreen.SelectUnitActivity" />
        <activity android:name=".screen.addfooditemscreen.AddFoodItemsActivity" />
        <activity
            android:name=".screen.selectfooditemscreen.SelectFoodItemActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>